{
  "name": "budmetal",
  "version": "0.1.0",
  "dependencies": {
    "bootstrap": "^3.3.7"
  },
  "scripts": {
    "serve": "lite-server -c=bs-config.json",
    "start": "npm run serve"
  },
  "devDependencies": {
    "lite-server": "^2.3.0"
  }
}
